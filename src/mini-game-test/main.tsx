import React from 'react'
import ReactDOM from 'react-dom/client'
import { MiniGameTestApp } from './MiniGameTestApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MiniGameTestApp />
  </React.StrictMode>,
)
